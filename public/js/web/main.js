/*
requirejs.config({
 paths: {

 },
 shim: {
 }
});
*/

require([
    'context',
    'controller'

], function (context, controller) {

    $(function () {

        initBody();

    });

});

function initBody() {
}
