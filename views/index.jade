extends layout

block header
    - if (showHeader)
        h1 Header Block #{showHeader}
        p Blablablabla Hello world
        a(href='/news') Aktuelles

block content
  h1= title
  p Welcome to #{title}
  p App mit Express, Jade, Node, Less und Heroku

  h2 Login

  form(id='form1', method='get', action='/form_validation')
    input(type='text', name='name', value='', id='inp_name')
    input(type='submit', value='Abschicken')


